daoproperties:
  filePath: questions.csv

spring:
  messages:
    basename: messages
    encoding: UTF-8
    fallback-to-system-locale: true
  shell:
    interactive:
      enabled: false
  main:
    allow-circular-references: true